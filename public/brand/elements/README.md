# /brand/elements — isolated objects only

- Transparent PNG preferred (or flat black #0a0f1a / pure black for cutout later)
- **No text, no logos with wordmarks, no full page chrome**
- One subject per file, generous padding, ~512–1024px
- Named: `{subject}-{variant}.png` e.g. `pin-gold.png`, `brain-blue.png`, `nodes-gold.png`

Grok shares Imagine URLs → Marvin curls into this folder.
