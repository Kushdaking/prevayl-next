# 28 live-page redesigns

All routes below read **preserved copy** from prevaylos.com (redesign only — no invented numbers).

## Class contract
`prevayl-gold`, `prevayl-navy` / `-2` / `-3` / `-4`, `prevayl-soft`, `prevayl-muted`, `.glass`, `.glass-strong`, `.text-gold-gradient`, `font-display`, `Band` (`image?`, `align`, `height`).

## Marketing vs docs
- **Marketing** (`MarketingPage` + `Band`): platform modules, solutions, about, customers, resources hub/blog/security.
- **Docs** (`DocsShell` + `DocPage`): dense sidebar, code blocks, scannable H2/H3 — **no full-bleed image bands**.

## Platform (10)
| Route | Notes |
|-------|-------|
| `/platform/dispatch` | Full live sections |
| `/platform/carriers` | Free forever preserved |
| `/platform/compliance` | DOT/FMCSA/eBOL — no fake certs |
| `/platform/ai` | 17 workflows / 15 agents from live |
| `/platform/agentic-ai` | Trust-layered personas |
| `/platform/business-intelligence` | 11 metrics, 7-rule anomaly engine |
| `/platform/load-optimizer` | **Kept** (deep link) |
| `/platform/route-optimizer` | **Kept** |
| `/platform/flolens` | **Kept** |
| `/platform/pin-protocol` | **Kept** |

### Decision: four nav-orphan platform pages
`load-optimizer`, `route-optimizer`, `flolens`, `pin-protocol` **survive** — real live content. Linked from Platform dropdown. Remove only if product kills modules.

## Solutions / Resources / About / Customers
See `content/pages/*`. Docs ship-status (SHIPPED / PLANNED Q3 2026) from live docs only.

## Edit rule
Change copy in `content/pages/*` only. Do not invent MC/DOT or volume stats beyond live site.
