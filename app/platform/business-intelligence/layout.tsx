import type { ReactNode } from "react";
export const metadata = { alternates: { canonical: "https://prevaylos.com/platform/business-intelligence/" } };
export default function Layout({children}:{children:ReactNode}){return children;}
