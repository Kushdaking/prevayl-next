import type { ReactNode } from "react";
export const metadata = { alternates: { canonical: "https://prevaylos.com/customers/" } };
export default function Layout({children}:{children:ReactNode}){return children;}
