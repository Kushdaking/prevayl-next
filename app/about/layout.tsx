import type { ReactNode } from "react";
export const metadata = { alternates: { canonical: "https://prevaylos.com/about/" } };
export default function Layout({children}:{children:ReactNode}){return children;}
