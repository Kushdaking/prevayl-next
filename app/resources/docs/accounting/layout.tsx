import type { ReactNode } from "react";
export const metadata = { alternates: { canonical: "https://prevaylos.com/resources/docs/accounting/" } };
export default function Layout({children}:{children:ReactNode}){return children;}
