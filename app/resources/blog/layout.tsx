import type { ReactNode } from "react";
export const metadata = { alternates: { canonical: "https://prevaylos.com/resources/blog/" } };
export default function Layout({children}:{children:ReactNode}){return children;}
