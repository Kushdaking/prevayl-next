import type { ReactNode } from "react";
export const metadata = { alternates: { canonical: "https://prevaylos.com/solutions/dealers/" } };
export default function Layout({children}:{children:ReactNode}){return children;}
